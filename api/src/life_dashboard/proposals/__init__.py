"""Agent proposals — the middle outcome between "can" and "can't" (proposal-001).

See ``plans/open-hearth/agent-proposals.md``. A write authorized at the
``propose`` tier is recorded here as a pending :class:`~.models.Proposal` for a
human to approve or reject, instead of executing or 403-ing.
"""
