{
  "name": "life-dashboard-desktop",
  "version": "0.1.0",
  "description": "Hearth desktop app (Tauri shell)",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:web": "cd ../web && TAURI=1 NEXT_PUBLIC_API_BASE_URL=http://localhost:1338 NEXT_PUBLIC_TAURI=true npm run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
